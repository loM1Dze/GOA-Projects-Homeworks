; project/
; │
; ├── index.html        ← ლუკა (მთავარი გვერდი)
; ├── about.html        ← რეზი (ჩვენს შესახებ)
; ├── services.html     ← გიორგი (სერვისები)
; ├── contact.html      ← გიგა (კონტაქტი)
; │
; ├── css/
; │   └── style.css     ← ნიკა (ყველასთვის საერთო სტილები)
; │
; └── images/           ← რეზი (სურათები, ლოგოები და ა.შ.)
